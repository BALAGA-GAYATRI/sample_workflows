# This is a basic workflow to help you get started with Actions

name: issue_dashboard

# Controls when the action will run. 
on: [workflow_dispatch]


# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: 'Generate Dashboard'
      id: dashboard
      uses: ethomson/issue-dashboard@v1
      with:
        config: |
          title: 'Dashboard'
          output:
            format: markdown
            filename: 'dashboard.html'
          sections:
          - title: 'Pull Requests'
            widgets:
            - type: 'number'
              title: 'ligbit'
              issue_query: 'repo:libgit2/libgit2 repo:Azure/login is:open is:issue created:>{{ date("-365 days") }}'
        token: ${{ github.token }}

    - name: Commit files
      id: commit
      run: |
        git config --local user.email "balaga-gayatri@github.com"
        git config --local user.name "balaga-gayatri"
        git add --all
        if [-z "$(git status --porcelain)"]; then
           echo "::set-output name=push::false"
        else
           git commit -m "Add changes" -a
           echo "::set-output name=push::true"
        fi
      shell: bash
 
    - name: Push changes
      if: steps.commit.outputs.push == 'true'
      uses: ad-m/github-push-action@master
      with:
         github_token: ${{ secrets.GITHUB_TOKEN }}
